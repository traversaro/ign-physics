/* Config.hh. Generated by CMake for @PROJECT_NAME_NO_VERSION@. */

#include <ignition/physics/Export.hh>

#include <string>

#ifndef IGNITION_${GZ_DESIGNATION_UPPER}_CONFIG_HH_
#define IGNITION_${GZ_DESIGNATION_UPPER}_CONFIG_HH_

/* Version number */
#define IGNITION_${GZ_DESIGNATION_UPPER}_MAJOR_VERSION ${PROJECT_VERSION_MAJOR}
#define IGNITION_${GZ_DESIGNATION_UPPER}_MINOR_VERSION ${PROJECT_VERSION_MINOR}
#define IGNITION_${GZ_DESIGNATION_UPPER}_PATCH_VERSION ${PROJECT_VERSION_PATCH}

#define IGNITION_${GZ_DESIGNATION_UPPER}_VERSION "${PROJECT_VERSION}"
#define IGNITION_${GZ_DESIGNATION_UPPER}_VERSION_FULL "${PROJECT_VERSION_FULL}"

// Deprecated: use ignition::physics::getEngineInstallDir() instead
#define IGNITION_PHYSICS_ENGINE_INSTALL_DIR "${IGNITION_PHYSICS_ENGINE_INSTALL_DIR}"

#define IGNITION_PHYSICS_ENGINE_RELATIVE_INSTALL_DIR "${IGNITION_PHYSICS_ENGINE_RELATIVE_INSTALL_DIR}"

namespace ignition
{
  namespace physics
  {
    /// \brief getInstallPrefix return the install prefix of the library
    /// i.e. CMAKE_INSTALL_PREFIX unless the library has been moved
    IGNITION_PHYSICS_VISIBLE std::string getInstallPrefix();

    /// \brief getEngineInstallDir return the install directory of the engines
    IGNITION_PHYSICS_VISIBLE std::string getEngineInstallDir();
  }
}

#endif
